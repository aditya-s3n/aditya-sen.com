# Textures

Nothing here is required: the ocean (`world/Ocean.ts`) computes its waves
procedurally, and the bridge uses flat materials.

Optional ideas:
- `waternormals.jpg` if you swap Ocean.ts for three's `Water.js` addon
  (copy it from the three.js repo, `examples/textures/waternormals.jpg`).
- Tileable concrete/steel detail maps for the deck and towers.
